# score-transcriber environment template
# 复制为 .env 并填写。.env 已被 .gitignore 忽略，请勿提交真实密钥。

# GitHub PAT（repo + workflow 作用域），用于本地 git 推送 / CI 调试
GITHUB_TOKEN=

# 后端监听端口（run.py 读取 PORT，默认 8000）
PORT=8000
