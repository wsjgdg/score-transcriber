# 运行期完整依赖（部署/沙箱用）。CI 只需轻量子集，见 requirements-ci.txt。
# 注意：basic-pitch 需配合 jax==0.4.38 以兼容 numpy 1.26 / tensorflow 2.15。
basic-pitch==0.4.0
music21==10.5.0
pretty_midi==0.2.11
fastapi
uvicorn
python-multipart   # FastAPI Form/File 上传端点（/api/transcribe）import 时即要求安装
matplotlib
numpy==1.26.4
soundfile        # 顶层硬依赖（transcriber.py: import soundfile），缺则无法 import 模块
librosa          # 函数内惰性导入，运行时做 STFT / 节拍跟踪 / BPM 估计
jax==0.4.38
demucs==4.1.0   # 可选：分离伴奏预处理（默认开启），依赖 torch（已随沙箱预装）
# faster-whisper==1.1.1   # 可选：自动识别人声歌词(ASR)，仅自托管 GPU 环境；免费托管未预装
# whisper==20231119       # 备选 ASR 后端（OpenAI 官方实现）

# --- 识谱成曲（OMR，图片识别，可选）---
# 简谱 OMR：本地 OCR 引擎二选一
paddleocr          # 简谱 OCR（中文印刷体更准）；或改用下方 pytesseract
# pytesseract        # 简谱 OCR 备选；系统需另装 tesseract-ocr
# 五线谱 OMR：开源引擎 Audiveris（Java），无需 pip 安装，运行时自动探测 audiveris(.jar)；
#   缺失时接口返回友好提示，见 omr_staff.find_audiveris()
# 解析 MusicXML 产物复用上方已列出的 music21==10.5.0
