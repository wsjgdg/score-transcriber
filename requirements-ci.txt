# Minimal deps for CI: app import check + route health check + unit tests.
# Heavy ML deps (basic-pitch/tensorflow, music21, pretty_midi, demucs/torch, jax)
# are lazy-imported inside transcriber.py and NOT required by the test suite,
# so installing them here would only slow CI down / risk build failures.
numpy==1.26.4
soundfile
matplotlib
fastapi
uvicorn
python-multipart   # FastAPI Form/File 上传端点（/api/transcribe）import 时即要求安装
pytest
httpx
